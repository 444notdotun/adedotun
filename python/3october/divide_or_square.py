def divide_or_square(number):
	import math
	if number == type(string):
		return f"invalid input"
	elif number < 0:
		return f"invalid input"
	else:

		if number % 5 == 0:
			
			solution = math.sqrt(number)
		else:
			solution  = number % 5
	
		return solution



print(divide_or_square("hello"))
	